NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_WRITE_TOKEN=

# Sanity Studio deployment the "Write" action in the main navigation opens.
# Leave empty to render an in-page notice describing how editorial access is
# granted instead of a dead link. NEXT_PUBLIC_ is required because the value is
# read at build time and must not be treated as a secret.
NEXT_PUBLIC_SANITY_STUDIO_URL=
